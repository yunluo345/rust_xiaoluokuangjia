在创建文件和编写代码时，必须严格遵守当前后端项目的架构规范、代码规范以及我自定义的命名规则，包括但不限于：文件必须创建在指定目录结构中，严格遵循分层架构原则（controller / service / repository / domain 等），不得随意新增不符合规范的目录；类名、方法名、变量名必须符合现有命名风格，避免随意缩写，命名语义清晰且具备良好可读性；禁止随意硬编码，所有常量必须统一抽取至常量类或集中管理位置；优先考虑代码复用性，合理拆分方法并提取私有方法，避免重复代码（DRY 原则），遵循单一职责原则（SRP），坚持面向接口编程；禁止使用低性能或低质量实现方式，优先采用时间复杂度更优的算法，实现应优雅、可扩展、可维护，必要时合理使用缓存、批处理、流式处理等优化方案；保持代码简洁，避免过度嵌套，合理使用函数式写法（在适用场景下），异常处理规范化且不得吞异常；整体代码结构必须清晰，逻辑层次分明，具备良好的可扩展性与可维护性。


现在wasm支持吗？让前端也支持上，然后看看是否遵守我的规则是否优雅